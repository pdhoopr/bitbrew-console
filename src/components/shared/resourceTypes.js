const resourceTypes = {
  destination: "destination",
  device: "device",
  org: "organization",
  orgMember: "member",
  project: "project",
  rule: "rule",
};

Object.freeze(resourceTypes);

export default resourceTypes;
