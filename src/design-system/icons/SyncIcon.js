import styled from "styled-components";
import SyncSvg from "./assets/sync.svg";
import iconStyles from "./iconStyles";

export default styled(SyncSvg)`
  ${iconStyles};
  fill: var(--color-green);
`;
